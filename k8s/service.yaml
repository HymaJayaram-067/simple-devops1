apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
  namespace: devops-app
  labels:
    app: simple-app
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    nodePort: 30080
  selector:
    app: simple-app
